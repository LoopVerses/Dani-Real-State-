import { FINAL_IMAGE_FRAME } from "@/lib/images";

/** Homepage grid cards — matches final_images 409×320 */
export const HOME_CARD_IMAGE_FRAME = FINAL_IMAGE_FRAME;

/** Hero slides — image area below fixed navbar so banner text stays visible */
export const HERO_IMAGE_AREA =
  "absolute inset-x-0 bottom-0 top-[58px] overflow-hidden sm:top-[62px]";
